export enum HealthCheckTemplateType {
  GENERAL = 'GENERAL',
  DENTAL = 'DENTAL',
  VISION = 'VISION',
  PSYCHOLOGICAL = 'PSYCHOLOGICAL',
}

export enum CheckupItemDataType {
  NUMBER = 'NUMBER',
  TEXT = 'TEXT',
  BOOLEAN = 'BOOLEAN',
  SELECT = 'SELECT',
}